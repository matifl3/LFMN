spring.application.name=lfmNacional

spring.datasource.url=jdbc:mysql://localhost:3306/lfm?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:changeme}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

server.port=8080

sesiones.input-dir=${SESIONES_DIR:C:/Facultad/ProyectoProgra/LowFuelMotorSport Local/lfmNacional/sesiones}
sesiones.procesadas-dir=${SESIONES_DIR:C:/Facultad/ProyectoProgra/LowFuelMotorSport Local/lfmNacional/sesiones}/procesadas
sesiones.errores-dir=${SESIONES_DIR:C:/Facultad/ProyectoProgra/LowFuelMotorSport Local/lfmNacional/sesiones}/errores

archivos.base-dir=./archivos

frontend.resources-path=file:../files/

steam.realm=${FRONTEND_URL:http://localhost:8080}
steam.return-to=${FRONTEND_URL:http://localhost:8080}/api/steam/vinculacion/callback
steam.return-to-auth=${FRONTEND_URL:http://localhost:8080}/api/steam/auth/callback
frontend.url=${FRONTEND_URL:http://localhost:8080}
steam.state-timeout-minutos=10

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

jwt.secreto=${JWT_SECRETO:lfmNacional-clave-jwt-2026-muy-larga-y-segura-para-firmar-tokens-hs256}
jwt.expiracion-minutos=480

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
